# M21 — model selector and complete expert context

Implementation branch: `m21-model-selector`. GitHub `ci` is the release authority.
Requirements: REQ-ENG-004/005/009/011, REQ-FIN-002, POC-F1/F2/F4/F5.

The public selector exposes `atlas`, `atlas-glm`, and `atlas-fast`, mapped to
three configured gateway roles. All generation calls explicitly request
`reasoning_effort: none`, including recovery and evaluation. The abandoned
deep alias is rejected. Limits are 0.10 EUR,120 seconds,3000 output tokens and
10 tools per request; actual provider identity and measured usage are exposed.
The shared selector contract is `contracts/m21-profiles.json`; physical model
identifiers and role mapping remain gateway configuration.

CONTRADICTION: the owner's latest three-model decision supersedes the old
reasoning-mode contract, which is archived verbatim in `docs/decisions-log.md`.
CONTRADICTION résolue en autonomie: the newer PoC page-read allowance is30s
instead of15s; the120s request deadline is retained. All three non-reasoning
profiles retain the standard0.10 EUR cap. The current mandate authorizes squash
merge only after the exact PR head's GitHub `ci` job is green.

Full extracted pages and retrieved chunks now reach context budgeting without
per-source byte cuts. Current-turn images are stored and propagated by reference;
vision requests use high detail within conservative image-token reservations.
Prompts ask for derivations, source-specific evidence and explicit conclusions.
The UI displays elapsed activity, named expandable tool steps and green user
messages. Empty/malformed replies receive bounded recovery. A failure after
visible output terminates explicitly; independent answers are never concatenated.
Errors include measured elapsed time and reserved cost, excluding private upstream
exception text. Existing isolation, source-safety and budget assertions remain.

## Evidence

The completed M21 acquisition replayed an exact recorded134.65s prefix and then
acquired fresh high-detail photo, article and RAG exchanges. A later exact prefix
through the corrected photo was reused while acquiring the final article/RAG suffix. The committed cassette
contains actual external responses and recorded stream delays, with provider model
identity included in request matching. Deterministic verification replays these exact exchanges.

| Journey | Measured cost EUR | Result |
|---|---:|---|
| J27 atlas | 0.010911 | useful same-question comparison |
| J27 atlas-glm | 0.0115977 | useful same-question comparison |
| J27 atlas-fast | 0.0077756 | useful same-question comparison |
| J29 photo | 0.002679 | original expert/geometry assertions satisfied |
| J30 article | 0.006624 | original article details retained |
| J31 RAG | 0.0020736 | fact beyond byte400 and citation retained |

J28's deliberate one-token limit produces an explicit bounded error. The original
stricter J27 instruction/timing derivation metric remains **false for all three
models** and is reported separately from the owner's revised selector gate. This
is a quality limitation, not evidence that the technical derivations are correct.
Earlier failed acquisitions are not release evidence. One final article attempt
received robots_unavailable and produced an unsupported answer; the unchanged
article-details assertion rejected it. The next live fetch succeeded. A proposed
historical-source fallback was unnecessary and was removed. Runtime robots checks
remain unchanged; continuous upstream availability is not established by replay.

Regression images were acquired on two sequential temporary L4 workers at0.7875
EUR/h each. The second was needed because the first was shut down before the
remaining M19 generation cases had been read. M20
refresh combines actual current inference/images with exact historical startup
responses, explicitly recorded as mixed acquisition. Deterministic CI replays these
real exchanges. The worker, root disk and IP were removed via `infra/gpu-down.sh`;
both workers were removed and all nine zones show no GPU, with only the existing BASIC3-X4C-16G CPU remaining.

Focused vision output required a final correction: general chat guidance precedes
vision-specific scope, and simple property questions receive one direct sentence.
Two earlier M12 acquisitions answered blue/right correctly but added an unwanted
inverse left-square relation; their unchanged assertion rejected them. The corrected
acquisition and protected replay both succeeded. Changed vision requests were
reacquired; acquisition-only reuse accepts unchanged request values once per row.

M10's harness had paired the public profile with a legacy0.05 EUR loop budget. It
now supplies the public0.10 limit; its original formula, evidence and cost assertions
remain. M7's real provider fixture was also refreshed for the new request shape.
An M20 startup check collided with another local listener during concurrent
acquisition; its subsequent sequential startup check completed successfully.

An isolated browser check with a synthetic test provider measured activity at1.221s,
verified updating elapsed text and rgb(217,243,223) user-message background, and
showed native collapsed reflection/page-reading controls. Screenshots are in BRAIN;
the temporary UI container was removed. No browser dependency was added.

Local `make verify-m21` exited0 with J1–J33 complete in replay mode; the optional
standalone live probe was skipped because no persistent stack was running. Live
acquisitions above used the actual HTTP stack.439 unit tests exited0, along with
lint, strict typing and secret scan. GitHub CI remains the milestone authority. CI reuses only complete passing
J1–J33 replay evidence for identical source and environment within the same run;
each protected verifier still checks its assertions. Failed/incomplete results are
never cached, and local or acquisition runs always execute the full suite.

Source: [Scaleway pricing](https://www.scaleway.com/en/generative-apis/), checked
2026-09-17: fast role0.40/0.80 EUR per million input/output tokens, no cache discount.
No protected file changed. No later milestone is listed.

## Owner-supplied measurements (not measured by this session)

### Reasoning mode: negative result confirmed on all three provider models (2026-09-17)
Same hard Z80 question, `max_tokens` 12 000, `reasoning_effort` explicit:
| model | none | high |
|---|---|---|
| qwen3.5-397b | 6 888 chars, 2 069 tok | wrong timings, then empty answers, then tool JSON |
| glm-5.2 | 4 563 chars, 1 413 tok, 40 s | **non-JSON reply** after 107 s |
| deepseek-v4-flash | 4 628 chars, 1 489 tok, **19 s** | 42 081 chars of trace, **0 chars of answer**, 12 000 tok saturated, 204 s |
Reasoning mode is unusable on this provider regardless of model. Standard mode works on
all three. Record this table in `reports/M21.md`. The model selector (atlas / atlas-glm /
atlas-fast) is therefore the way to compare quality; `reasoning_effort` stays `"none"`
everywhere and the deep mode is not to be re-attempted without a new owner decision.


## Evaluation regression compatibility

The full CI pipeline exposed two legacy integration assumptions. The M4 routing
launcher now includes the repository root on PYTHONPATH for the shared selector
contract. M5's RAG evaluations were reacquired for the new prompt, preserving all
existing score/citation/calibration assertions and the0.05 EUR/call,3 EUR/series caps.

The old reference judge cannot disable reasoning, as documented by
[Scaleway](https://www.scaleway.com/en/docs/generative-apis/how-to/query-reasoning-models/).
The reference therefore defaults to the configured fast model, distinct from the
system and production judge, with optional `REFERENCE_JUDGE_MODEL` override.
Every evaluation call still sends explicit none. Replay requires the configured
model identity; old-judge rows cannot silently substitute for the current judge.
New reference calibration is acquired and reported separately from historical
measurements. A regression test rejects model-mismatched replay without network I/O.

Final local evaluation acquisition satisfies all existing PoC thresholds:
E2=0.9933,E3=1.0,E5=1.0. Calibration has30 pairs,κ=0.7872 and no exclusions.
The archive contains369 actual exchanges with cumulative recorded successful-call
cost0.71835705 EUR; this includes retained historical rows, not just this run.
A conservative0.05 EUR allowance also covers the failed old-reference request
whose usage was unavailable. The broader quality_go remains false for the
previously documented language gaps. No language, score or refusal assertion was
relaxed. The earlier E3 format failure is corrected by giving source sufficiency
priority over expert style, and legacy M2 replies were reacquired for that prompt.

M12's standalone replay gateway also needed the configured text-role environment
that the normal launcher supplies. CI reproduced a missing ESCALATION_MODEL;
the harness now supplies the gateway default when absent. Credential-free replay
exercises the same two HTTP requests and unchanged content/log assertions.
