# M19 — explicit intent, language and locked settings

M19 D1–D4 are implemented. `generate_image(prompt)` is selected by the chat
model, including scoped project conversations. Image execution shares the
remaining request budget and deadline; uploaded-image analysis keeps its vision
path. Capability declarations, explicit resolved language and permanent settings
locks cover chat, vision, web, rewriting and follow-up paths.

Requirements: MISSION M19/J15–J20, REQ-ENG-004/009/011, REQ-FIN-002,
POC-F1/F5/P6. No protected file was changed, dependency added, assertion weakened
or budget increased. The existing licence/model and generation settings remain.

Public API evidence comprises 53 real provider exchanges and ten generated
images. The exact recorded prefix from the earlier L4 session is replayed;
J12 recovery and J15–J20 were recorded after SerpApi recovered. The injected first
J12 timeout remains, followed by a successful real search and sourced answer.
French and English drawing phrasings select the tool. Analysis never generates;
short accented/unaccented French inputs receive French descriptions. Capability
answers acknowledge generation, and the web/image conversation stays in French.
All J1–J7/J9–J20 assertions completed; J8 separately verified two consecutive
service starts. The final archive is replayed through the public HTTP API in CI.

J14 retained all three constraints according to its vision assessment. Prompt:
“Crée une image d'un chat dansant avec un chien, tous deux portant des patins à
roulettes et des lunettes roses.” Rewritten prompt:

```text
Subjects: A cat and a dog dancing together.
Attributes: Both the cat and the dog are wearing roller skates and pink glasses.
Scene: A dynamic setting suitable for dancing.
Style: Vibrant and playful illustration.
```

Seed: 1079749996. Dancing animals: true; roller skates: true; pink glasses: true.
This is one measured sample, not a guarantee of all future constraint fidelity.

The L4 costs EUR 0.7875/hour excluding storage/IP. BF16 weights exceed its usable
22 GiB VRAM, so the worker uses sequential CPU offload below 40 GiB and resident
placement on larger GPUs. Tests cover both placements and the HTTP protocol.
All generated requests remained under the existing EUR 0.05 ledger. GPU cleanup
completed; the final dedicated inventory contains only the CPU VM. Persistent
weights were retained; transient storage/IP costs were not measured.

The prior recording regression failed before its exact expected tool list was
updated to include the declared image tool. The public tool-routing regression
also failed before implementation. Project replies must still validate the exact
JSON contract, including unknown answers; no isolation validation was relaxed.

Local evidence: 339 tests, lint, typecheck, secret scan, J8 and `make verify-m19`.
CI remains the completion authority. Publication is split into worker memory,
model-selected execution and final journey verification to keep review increments
below 400 changed text lines. No next milestone is defined in MISSION.
