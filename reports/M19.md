# M19 implementation and verification status

M19 is incomplete. The contract is merged in PR #91. The language increment is
in PR #92; its first CI run failed M10 and its compact-prompt correction is awaiting CI. Image-tool and public-journey changes are prepared on separate branches.

Implemented: model-selected image generation with a shared cost/deadline ledger;
explicit capabilities and resolved language; source-language-aware image rewriting;
permanent settings locks; public J15–J20 and English generation cases; standard
`verify-m19` integration. Project history remains scoped before routing.

Local validation exited 0: 337 unit tests, lint, type checking and secret scan. `verify-m19` reaches all static
locks but cannot pass with the old provider recordings. New GPU-generated images
and the corresponding exact provider exchanges remain required. No protected file
was modified and no threshold or budget was raised.

Blocked after three provisioning attempts. The final attempt created an affordable
L40S server, but Scaleway refused poweron with `resource out of stock`. The server
never ran. Cleanup removed the server, its IP, root disk and security group; the
pre-existing weights volume is retained. No fourth attempt was made.

The new M7 exchange was recorded against the real provider (EUR 0.0127899).
Image-tool J1–J20 recordings remain missing; `verify-m19` is not green and M19
is not complete. The repeated-image-edit regression is covered by a test that
failed before the fix; the original natural-language subject is now retained.

CONTRADICTION: M16 rejects French prose in MISSION. Exact French acceptance inputs
are preserved in the journey fixture and linked from English mission descriptions.
