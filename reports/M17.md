# M17 — Public chat integration

Requirements: MISSION M17 J1–J8, POC-F1/F3/F5, REQ-ENG-009.
Local verification: public HTTP J1–J7 replay and separate J8 completed; commit-specific CI evidence is attached to the delivery PRs.
Project scope is resolved before vision. Only server conversation history and the current upload reach vision; descriptions are stored without image bytes.
J5 uploads an image in Alpha through the API, switches to Beta with Alpha history present, and requires an isolated answer and exact recorded provider requests.
Journeys also verify French text/vision, resolvable corpus citations, a 512×512 generated PNG, shared project facts, and web search/fetch followed by synthesis.
Auxiliary title/tags/follow-up/autocomplete generation is explicitly disabled; see `runbooks/chat.md` for project commands and startup instructions.
Provider/image recordings are real, archived from VM runs; CI replays them. The additional Alpha vision exchange was recorded live, with all other exchanges replayed.
Image generation requires a warm GPU; the recording GPU was destroyed. No GPU was created during the scope-fix session.
No threshold or protected assertion was relaxed. Reservation covers each actual primary/fallback pair within the unchanged 0.05 EUR request cap.
Language checks use seeded langdetect 1.0.9 (MIT, approximately 2.38 MB plus six), confidence >=0.8, excluding code; negative language/code tests remain enforced.
J8 exposed a teardown/container-removal race, now covered by regression tests and two consecutive successful local starts. It is excluded from CI.
