# M15 — API développeurs

REQ-DEV-001..005, AC-DEV-001..004 ; contrats PR71/75 mergés avant code.
L'entrée locale8030 propose Chat Completions, Responses et Messages, modèle code
par défaut, sans accès aux données privées du chat8020. Clés hachées, quotas UTC
atomiques et persistants, réservations avant inférence, coût par développeur.
Les outils sont exécutés par le client ; SSE texte et arguments incrémentaux.

## Mesures et preuves

`make verify-m15` local a exécuté le rejeu et le fournisseur réel : aller-retour
fonction/résultat739microEUR et grand contexte13050microEUR, trois appels en3,85s (dernière exécution).
Le contexte utile contient24616octets ; trois valeurs distantes sont vérifiées
exactement. Les dépassements contexte/coût sont refusés avant transport, sans
troncature. Le parseur du test tolère une enveloppe Markdown JSON entière ; aucune
valeur attendue n'est modifiée.

`make test-devapi-clients` utilise les véritables clients, versions vérifiées,
dans des conteneurs sans réseau ni clés fournisseur/GitHub. Le fichier produit est
évalué par quatre assertions externes dans un second conteneur sans clé.

| Client | Requêtes | Coût EUR | Sortie | Assertions |
|---|---:|---:|---:|---:|
| Codex0.153.4 | 4 | 0,017193 | 0 | 4/4 |
| Claude Code2.1.268 | 3 | 0,007533 | 0 | 4/4 |

Source: preuves synthétiques locales BRAIN/eval/devapi-clients.json ; aucun journal
d'interaction utilisateur utilisé. Réserves inconnues des clients : zéro.
Coût M15 majoré, diagnostics inclus :0,1173269EUR. Chaque requête reste sous0,05EUR.
Aucun GPU créé ; coût cloud additionnel0EUR/h.

Tests de régression : auth/quotas concurrents, annulation, timeout, troncature,
options invalides et objets mal formés ; flux fournisseur enregistrés dans tests.
Baselines documentées dans les PR : module absent, HTTP404, champ id rejeté,
cache_control rejeté, discriminateur d'outil absent. Assertions conservées.
Couverture mesurée des composants : auth86%, gateway94%, Chat92%, HTTP94%,
entrées91%, Responses100%, Messages98%, relais94%.
La CI exécute le vérificateur protégé via scripts/test.sh, sans appel payant.
La clôture et le lien du dernier job ci vert sont consignés dans BRAIN/JOURNAL.md.

## Limites et exploitation

RISK: la limite financière borne le contexte disponible sous la fenêtre annoncée
par le fournisseur ; aucun dépôt arbitraire n'est garanti sous0,05EUR.
Usage absent/annulation/redémarrage : réserve conservée. Une seule tentative code.
Pas de stockage Responses, d'outils hébergés ni de multimodal/thinking sur cette API.
La compatibilité Claude Code avec ce fournisseur est expérimentale ; versions
futures non garanties. Profils et création/révocation des clés : runbooks/devapi.md.
NOTICED BUT NOT TOUCHING: exposition publique/HTTPS et UI multi-utilisateur hors PoC.
CONTRADICTION: le mandat courant autorise le squash par l'agent après ci verte,
remplaçant le merge humain des instructions historiques. Aucun seuil assoupli.
