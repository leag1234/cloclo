# M22 — Verified research and graded vision

Requirements: MISSION M22 D1–D4, POC-F3/F4/F5,
REQ-ENG-004/005/009/011, REQ-FIN-002. Original owner cases and decisions
are preserved verbatim in tests/journeys/m22-cases.json and docs/decisions-log.md.

Submission status: implementation and local `make verify-m22` completed.
GitHub `ci` is the completion authority; exact-head CI and authorized squash
evidence are recorded on the PR and in BRAIN, without predeclaring success here.

Public changing-fact questions and explicit web requests receive mandatory discovery
before generation. Provided-URL practical questions first fetch the reference and
search for an independent guide. These calls use the existing tool, deadline and
money ledgers; internal evidence requests retain internal-first handling. Public
prompts require comparison of definitions, practical procedures and conflicting
sources. The public orchestration prompt is versioned separately from the legacy
internal prompt because those clients retain different context/budget contracts.

The vision role uses the owner-selected replacement and the previous vision role
as fallback; every text selector shares that vision route. Default text selection
is unchanged. Explicit non-reasoning generation and bounded recovery remain.
The provider's [published pricing](https://www.scaleway.com/en/pricing/model-as-a-service/)
was checked on 2026-09-17 before configuring the replacement's 0.25/0.50 EUR per
million input/output tokens. Public requests retain 0.10 EUR/120s/3000 output tokens;
legacy and image-operation allowances remain 0.05 EUR. No GPU was provisioned.

The original photo and owner frets 5,5,7,5,5,7 are in tests/vision_bench/. Inference
receives the same photograph and question for both roles, without annotations.
The scorer validates six ordered string/fret values, accepts a complete Markdown
JSON fence, and records structure/family independently. Family annotations are
explicitly derived from standard tuning, distinct from the owner's fret annotation.

| Vision role | Correct positions | Full barre recognised | Compatible family |
|---|---:|---|---|
| Previous | 0/6 | No | No |
| Selected | 0/6 | No | No |

This satisfies only the specified non-regression comparison. It demonstrates no
fret-reading improvement and does not support a chord-accuracy claim.

Planning sentences are filtered across stream boundaries and from stored answers;
activity events and journal counts record removal. Tests preserve substantive text,
quoted text and fenced code. Language checks classify answer prose rather than
source titles or tool-activity markup, retaining the 0.8 confidence threshold and
rejecting English prose masked by French activity labels. Initial image requests
are preserved before rewriting even if the selection model paraphrases them.

The mandatory-discovery detector is lexical and multilingual; prompts cover the
broader semantic rule. Novel phrasings outside detection still depend on model
compliance. Benchmark coverage is one owner photograph and is intentionally weak.

Local validation: `make verify-m22` exited 0 with J1–J38 in replay mode and
positions 0 >= 0. The optional probe of an already-running stack was skipped;
the gate exercised isolated HTTP stacks. The 452 unit tests, lint, strict typing
(246 files), secret scan, M3 legacy replay, M16 documentation and evaluation smoke
also completed locally. Ten recording tests cover tool errors and interrupted streams.
The integrated gate initially exposed a missing real fetch-error recording; the
recorder now uses the existing error capture and that exchange was reacquired.
Recorded external responses are never edited to satisfy assertions. Acquisitions
reuse only exact requests; existing generated-image recordings retain their actual
GPU provenance. CI is replay, not a fresh live-provider evaluation. Prior milestones'
quality limitations remain in their reports.

CONTRADICTION: stale M21-only state was superseded by the explicit M22 mandate.
The owner corrected the protected selector gate before this session. No protected
file or assertion threshold was changed by this implementation.
Source: repository contracts, public HTTP journeys and real captured exchanges.
