# M23 — readable rendering and complete attachments

Validation at publication: local evidence below. Milestone acceptance requires the implementation PR’s exact-head green `ci`; visual judgement remains the owner’s.

## Theme inspection before implementation

Source: running atlas-chat-ui container, Open WebUI v0.11.3, inspected on
2026-09-18. The served build `_app/immutable/assets/0.DgJjT2oH.css`
exposes `--font-sans` with `var(--app-font-family, -apple-system)` as its
first fallback; `--font-primary`, `--default-font-family`, `--font-mono`,
`--default-mono-font-family`, `--app-text-scale`, `--tw-prose-body`,
`--tw-prose-invert-body`, `--tw-prose-code`, `--tw-prose-pre-bg` and the
`--color-gray-*` palette. Source maps expose CodeBlock.svelte and
StatusHistory.svelte; middleware forwards upstream SSE `event` objects.

The stylesheet preserves the theme user-font variable and centralizes the serif
default in `--atlas-body-font`. Geometry is shared across light/dark modes;
foreground and code colors inherit themed variables. User-message rules retain
M21 values. Copy remains keyboard accessible via focus-within.

Version-fragile selectors: `.markdown`, CodeMirror `.cm-*`, `.copy-code-button`,
`dir=ltr` code wrappers and the structural toolbar/language-wrapper selectors.
After an upgrade inspect CodeBlock.svelte (served source map or upstream source),
check wrapper nesting, copy control and spacer, and update these scoped rules.
No hashed utility selector is used. Theme variable names also require checking
against the served CSS. Owner visual acceptance remains pending.

## Decisions, scope and evidence

CONTRADICTION résolue en autonomie: docs/13 retains the legacy 0.05 EUR request
threshold; MISSION and delivered public-profile golden journeys use 0.10 EUR.
Per the explicit night mandate, public profiles retain 0.10 EUR; M15 developer
requests retain 0.05 EUR. No ceiling, assertion or recorded provider reply was weakened.

The long-document path uses the existing conservative serialized-byte reservation.
A complete 173595-character/60-page PDF initially exceeded the selected role's
cost reservation although it fit context. When both configured expert roles cannot
fit, the gateway now considers already configured public text roles, preserving
all source text and the same 0.10 EUR cap. Existing affordable fallback priority is
unchanged. The measured full-document reply cost 0.0157368 EUR, without hierarchy
or corpus retrieval. This is a synthetic public test document with beginning,
middle and end facts, not the owner's unavailable original PDF.

Hierarchy uses the existing token estimator only above 262144 estimated context
tokens. That estimator never authorizes a paid call: gateway cost/context checks
retain their conservative byte bound and can explicitly refuse a larger input
within the estimated token window. Source:
[Scaleway token definitions](https://www.scaleway.com/en/docs/generative-apis/faq/).

J39 initially failed twice on real responses (French identifiers/docstrings).
The trailing language instruction was ambiguous about code. It now explicitly
restricts localized language to prose, distinguishing docstrings and developer
errors. The third acquisition validated all three public profiles. Failed raw
acquisitions remain in local BRAIN; successful exact exchanges are versioned.
Six natural phrasings now cover French, uppercase unaccented French, English, German, a three-word request and requests without the word Python. All retain the English-code and localized-prose assertions. Uppercase-only language detection now normalizes case before detection; the trailing current-question instruction explicitly scopes language to prose.

Local lint/typecheck completed. The unit suite executed 461 tests: 460 succeeded in the ordinary run; the unchanged image HTTP test collided with an unrelated container on port 8000 and succeeded when rerun in an isolated loopback network namespace. The new complete-document HTTP
test fails against the original prepare_uploads function (400 rather than 200),
proving the regression. M23 protected static gate and initial full HTTP replay
completed locally; GitHub remains authoritative. Developer 24KiB live refresh
cost 0.013077 EUR, vision refresh cost 0.00102175 EUR. Earlier milestone rechecks
are limited to visible prompt-dependent recording changes and mandatory CI gates.
No GPU provisioned; retained image recordings reused only for identical inputs.

Contract PR: https://github.com/leag1234/cloclo/pull/101, merged as df7d47872a5a7400e27c4056c415c86764fb6c11 after [green exact-head ci](https://github.com/leag1234/cloclo/actions/runs/35324480610).

## Rendering review artifacts

[Light sample](m 23-light.png) and [dark sample](m 23-dark.png) use Chromium with the
inspected deployed theme CSS, the new override and a synthetic Python debugging
answer following the inspected code-block DOM. These are review samples, not the
owner's original comparison or proof of owner acceptance. Computed body: 17px,
29.75px line height, serif default,700px paragraphs; code retains available width.
Hover copy visibility was checked. Visual inspection caught dark headings retaining
the light theme foreground; the exposed heading/bold/quote/link variables now use
the themed foreground too. Both sample images were inspected after that correction.

Mixed photo+document requests fail explicitly (photo count and supported limit),
rather than silently dropping photos. Send photos separately. PDF text extraction
is not OCR; a page without a text layer is an explicit failure. DOCX has no intrinsic
page count, so its journal reports zero pages. The real native OpenWebUI upload/inlet/proxy check completed with both original files and all beginning/middle/end/second-file facts. Final local `make verify-m23` completed, including native UI replay. Implementation CI remains the acceptance authority.

Prompt-dependent regressions use reacquired real exchanges, with unchanged provider inputs reused only on exact equality. The budget-answer prompt no longer permits older training knowledge to override newer dated evidence; M22 search-first assertions remain unchanged. M7, M10, M21 and M22 final acquisitions completed. Developer requests retain their original message sequence after the small system policy, with the upstream payload hash and 24 KiB context checked. No new dependency was introduced.
