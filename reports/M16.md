# M16 — English localization

Requirements: MISSION M16, REQ-ENG-008/011, REQ-FIN-002.

The public repository now has a project README with an architecture overview,
installation and serving instructions, SSH access, milestone summary, and license
note. Public requirements, contracts, runbooks, service documentation, comments,
and user-facing messages are translated into English.

Translations are produced directly as complete files, sequentially, and reviewed.
Each file or small group has its own English commit. No segmented transport,
numbered lines, translation cache, or invariant-validation pipeline is used.
This localization is delivered together in one milestone PR as requested; its
file-by-file commits provide review boundaries for the documentation-wide change.

Runtime identifiers, API fields, enum values, filenames, and multilingual routing
expressions retain their existing meaning. Corpus and golden evaluation data are
not translated. Synthetic multilingual test inputs remain in their original
languages. One expected runtime refusal in a test cassette is translated; the
recorded provider response and the exact equality assertion remain unchanged.

The project UI labels and document language are English. Its request parameters,
including the existing document language value, retain their previous behavior.
No product provider prompts, budgets, thresholds, or authorization rules are changed.
Protected workflows, verification scripts, and CODEOWNERS are untouched.
The existing test launcher invokes the protected M16 gate in GitHub CI.

Local verification: lint, type checking, 302 unit/integration tests, secret scan,
the four project UI tests, and `make verify-m16` (exit 0). The protected M16 verifier rejected the partially
translated documentation during implementation. Final milestone completion is
established by the GitHub `ci` check on the PR delivering this report; local
results alone are not completion evidence.

Source: MISSION.md and the unchanged scripts/verify-m16.sh.
CONTRADICTION: the current explicit authorization permits a squash merge after
successful CI, superseding the historical human-only merge instruction for this
mandate. No threshold, case-count, or language-distribution relaxation is introduced.
NOTICED BUT NOT TOUCHING: historical specifications and milestone limitations are
translated as written; this milestone does not revise their technical decisions.

During additional local regression checks, inherited cloud credentials inadvertently selected the live M13 path and provisioned a GPU at 1.46988 EUR/hour (excluding storage/IP), below the 2 EUR/hour cap. The local process was interrupted and infra/gpu-down.sh was invoked immediately. This incident and cleanup evidence are recorded in BRAIN; the hourly estimate was not recorded in STATUS before that unintended creation.
Direct translation calls use the configured Scaleway gateway model and pricing,
with a reservation before every call, a ceiling of 0.05 EUR per call, and a
cumulative M16 ceiling of 3 EUR. Actual usage and unresolved reservations are kept
in the private BRAIN cost ledger. Product inference pricing is unchanged.

Cumulative M16 ledger, including earlier attempts: 0.4389774 EUR measured plus
0.1184220 EUR in unresolved reservations, giving an upper bound of 0.5573994 EUR.
Maximum reservation per call: 0.039282 EUR. The inventory before the additional regression check contained one existing CPU VM and no GPU instances. The post-cleanup inventory is recorded in BRAIN.

The first PR CI run failed M3 exact replay because E4-020 still expected the French application-generated stop message. Only that expected final text was translated; all provider bytes, outcomes and strict assertions remain unchanged. Both original and localized recording hashes are documented beside the cassette.

GPU cleanup completed successfully: the transient GPU, its root disk and IP were removed. The remaining weights volume predates this session (2026-09-09); it was retained. No GPU remains in the dedicated zone. Exact infrastructure billing is not yet available.
