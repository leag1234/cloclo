# M6 — Decision File
Requirements: POC-P1..P9, POC-F2/F3/F6, REQ-ENG-004/009/011, REQ-FIN-002.
Existing contracts: Query/Result M3, telemetry M5, commands and Markdown output
imposed by scripts/verify-m6.sh. No new API schema or migration.
Plan: Pure filtering of controls invisible to Query validation; serverless micro-bench
(one reference call then two waves of eight), recorded live RAG/web/fallback demo,
report built from these measurements and M5 evaluations.
Maximum reservation: 17×0.05 + 3×0.05 = 1 EUR. No GPU.
Archives are measurements, not simulated responses. Replay exists only in tests;
live commands use existing transports.
The filter does not claim to resolve semantic injection; Unicode, tabs, and
line breaks are preserved. Controls C0/C1, surrogates, and bidi overrides
are rejected before I/O. No user content in logs.
The report keeps P7/uptime 14 days unavailable if missing, and distinguishes
serverless from the local GPU model. An absence or a missed threshold imposes NO-GO;
no value is invented to make the decision file green.
Tests: rejections before network, unchanged multilingual inputs, insufficient demo
traces refused, absence of measurement => NO-GO; local then green CI job.
