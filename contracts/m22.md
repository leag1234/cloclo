# M22 — Verified evidence and measured vision

Source: owner-approved MISSION M22 D1–D4 and protected verify-m22, 2026-09-17.
Requirements: POC-F3/F4/F5, REQ-ENG-004/005/009/011, REQ-FIN-002.
No public request/response schema changes. Existing M21 budgets, explicit none,
source distrust, SSRF and ownership contracts remain binding.

Before asserting changing facts, use web_search. Training knowledge is a hypothesis;
if verification fails, report the failed operation and uncertainty. Distinguish
figures with different definitions or dates. For instructions, comparisons or
recommendations based on a single reference, search the subject beyond the URL
(one to three searches within the existing shared quotas), then synthesize the
answer to the question and identify conflicting evidence with URL/date citations.

All public photo requests use the gateway vision role, with the prior vision
model as its configured fallback. Text selectors retain their current defaults.
The benchmark uses the unchanged owner guitar photo and low-to-high frets
5,5,7,5,5,7; structure is a full barre at fret5 and compatible families derived from standard tuning: A minor or D dominant with omitted third.
Score string/fret pairs out of six, plus structure and family booleans. Compare
identical inputs on current and previous roles; never feed annotations to inference.
No absolute accuracy threshold; report uncertainty and measured non-regression.

Remove standalone first-person search/planning narration before answer emission,
including across streaming chunk boundaries. Surface activity separately. Preserve
quoted text, code and substantive explanations. A narration-only reply cannot
become a successful empty answer. Both stream and stored final answer are filtered.

Acceptance: useful public HTTP J34–J38, real provider/tool capture for replay;
J1–J33 remain mandatory. Unit tests cover chunk splits, quotes/code, substantive
negative cases, score validation and vision selection/fallback. Protected scripts
remain unchanged. Done requires local verify-m22 and green GitHub ci before squash.
