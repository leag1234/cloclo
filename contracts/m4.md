# M4 — Cascade S → L
Source: docs/13 POC-F6/E8, REQ-INF-003/004, REQ-ENG-004/006, REQ-FIN-002.
Contract and code in the same PR allowed by mandate M4.

POST /agent/complete retains the M3 contract. The gateway classifies user messages: explicitly simple short tasks to local; need for reasoning, tool history, or unknown input to Scaleway. The classifier is pure, without access to E8 labels; decision and reason logged without text. The routing.yaml configuration provides endpoints and models per environment. If local is absent, inaccessible, invalid, or expires, a single switch to L; no retry to L. Local attempt <=2 s and <=20% of remaining time, global deadline unchanged. L price reserved by existing harness; local without serverless cost. Explicit L errors according to M3; no dummy responses outside tests.

AC: simple/complex decision, Unicode, empty/giant, complex history, local endpoint actually closed, valid local response, local error/timeout, L failure propagated, envelope unchanged, and absence of secret in logs.
E8: accuracy report, matrix, breakdown by language, sous_routage_critique, statuses and decisions per case. Threshold >=0.85 and no critical under-routed. Human-validated E8 reference in commit 8167ca9.
CI tests without live LLM; transport test injected exclusively under tests/.
Plan: gateway, tests, Makefile, scripts/test.sh; protected files unchanged.
Additional infra cost 0 EUR/h, no GPU created. No new dependency.
Bonus UI non-blocking according to MISSION; no UI developed in this milestone.
